version: "3.9"

services:
  db:
    image: postgres:16-alpine
    container_name: faden_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "55433:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}" ]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy: 
     resources: 
        limits:
          memory: "512M"
          cpus: "0.5"
    networks: 
      - faden_net

  redis:
    image: redis:7-alpine
    container_name: faden_redis
    ports:
      - "6390:6379"
    restart: unless-stopped
    volumes:
      - cache_data:/data
    deploy: 
     resources: 
        limits:
          memory: "512M"
          cpus: "0.5"
    networks:
      - faden_net

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: faden_backend
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3011
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}

      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      REDIS_URL: redis://redis:6379

      ENCRYPT_KEY: ${ENCRYPT_KEY}
      SALT: ${SALT}

      JWT_SECRET_ACCESS_ADMIN: ${JWT_SECRET_ACCESS_ADMIN}
      JWT_SECRET_REFRESH_ADMIN: ${JWT_SECRET_REFRESH_ADMIN}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}
      FRONTEND_URL: ${FRONTEND_URL}

      APP_EMAIL: ${APP_EMAIL}
      APP_PASSWORD: ${APP_PASSWORD}

      MAIL_HOST: ${MAIL_HOST}
      MAIL_PORT: ${MAIL_PORT}
      MAIL_USER: ${MAIL_USER}
      MAIL_PASS: ${MAIL_PASS}
      MAIL_FROM: ${MAIL_FROM}

      CORS_ORIGINS: ${CORS_ORIGINS}
      IPGEO_API_KEY: ${IPGEO_API_KEY}
      FCM_SERVICE_ACCOUNT_JSON: ${FCM_SERVICE_ACCOUNT_JSON}

    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started

    ports:
      - "8000:8000"

    volumes:
      - ./uploads:/app/uploads
    deploy: 
     resources: 
        limits:
          memory: "2G"
          cpus: "1"
    networks:
      - faden_net

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: faden_pgadmin
    restart: unless-stopped
    ports:
      - "5070:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
    depends_on:
      db:
        condition: service_healthy
    deploy: 
     resources: 
        limits:
          memory: "512M"
          cpus: "1"
    networks:
      - faden_net

networks:
  faden_net:
    driver: bridge

volumes:
  db_data:
  cache_data:
  uploads_data:
